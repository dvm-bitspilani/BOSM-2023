import React from "react";
import * as styles from "../Styles/ErrorComponent.module.css"

const ErrorScreen = props =>{

    <div className={styles["overlay"]} onClick={props.onClose} aria-hidden="true" >
        
    </div>
};

export default ErrorScreen;